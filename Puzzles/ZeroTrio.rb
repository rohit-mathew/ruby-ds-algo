# Find all combinations of 3 numbers that sum to zero

def find_zero_trio(array, index, current_sum, current_count) 
  if current_count == 0 
    
  end
end

findZeroTrio([2, 3, 1, -2, -1, 0, 2, -3, 0])